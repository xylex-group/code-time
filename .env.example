# `PG_URL` should point to a Postgres database where `codetime_entries` is created.
# Uncomment or copy this file to `.env` before running the proxy.

# Optional overrides
#CODETIME_UPSTREAM=https://api.codetime.dev
#CODETIME_LOG_DIR=logs
PG_URL=postgres://user:pass@localhost:5432/codetime
